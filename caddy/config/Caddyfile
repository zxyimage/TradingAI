{
  # 全局设置
  admin off
  auto_https off
}

# HTTP 配置
:80 {
  respond "HTTP is working!"
}

# HTTPS 配置 (使用自定义证书)
:18443 {
  # 使用自定义证书
  tls /etc/caddy/certs/cert.pem /etc/caddy/certs/key.pem
  respond "HTTPS with custom cert is working!"
}
